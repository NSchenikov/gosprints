Спринт 6: Микросервисная архитектура и Docker

Задача:

    Разделить проект на два (или более) микросервиса:
        Task Service (CRUD, поиск, очереди, часть логики gRPC).
        Notification Service (ответственный за отправку WebSocket-уведомлений и/или email-уведомлений).
    Настроить взаимодействие сервисов через gRPC (или через kafka).
    Настроить работу statefull состояния сервисов (если есть внутренние воркеры)
    Настроить Docker Compose:
        Запуск всех микросервисов.
        База данных (PostgreSQL).
        Redis (если используется).
    При желании добавить мониторинг (Prometheus + Grafana) в Docker Compose, чтобы всё поднималось одной командой.

Цели:

    Освоить подходы к [микро]сервисной архитектуре (разделение ответственности, коммуникация).
    Научиться работать с Docker и Docker Compose для локального/тестового развертывания.
    Понять, как масштабировать систему (увеличивать количество экземпляров «Task Service» при росте нагрузки).

    Предполагаемая архитектура:
    gosprints/

├── task-service/ # существующий проект + адаптация
│ ├── cmd/
│ ├── internal/
│ │ ├── cache/
│ │ ├── grpc/
│ │ ├── handlers/
│ │ ├── kafka/ # продюсер событий
│ │ ├── queue/
│ │ ├── repositories/
│ │ ├── scheduler/
│ │ ├── models/
│ │ ├── metrics/
│ │ └── worker/
│ ├── database/ # подключение к БД
│ ├── migrations/ # SQL миграции
│ ├── api/
│ │ ├── proto/
│ │ │ ├── task/ # gRPC контракт
│ │ │ └── events/ # события для Kafka
│ └── go.mod
│
├── notification-service/ # новый сервис
│ ├── cmd/
│ ├── internal/
│ │ ├── handlers/ #WebSocket хендлеры
│ │ ├── kafka/ # consumer
│ │ ├── notifiers/ # email, ws (manager)
│ │ └── ws/
│ ├── api/proto/events/ # те же события
│ └── go.mod
│
├── api-gateway/
│ ├── cmd/
│ ├── internal/
│ │ ├── grpc/client/
│ │ ├── handlers/
│ │ ├── middleware/
│ │ └── router/
│ ├── pkg/auth/
│ └── go.mod
│
└── docker-compose.yml

---

проверить что воркер, изменяя статусы, отражает эти изменения в кэше,т.е. правильно ли работает инвалидация. Написать интеграционный тест на проверку инвалидации кэша.

!) ПО gRPC
создал proto-файлы и нужную архитектуру, подготовил структуру для gRPC-сервера и клиента, переписал handlers/tasks.go под gRPC (сохранил и прямой доступ через сервис)
Проблема: не установился protoc (скореее всего из-за проблем с совместимостью на старой macOS). Еще осталось сделать полнотекстовый поиск

upd:

1. при переносе на новый мак структура моих tasks в перенесенном postgre не сохранилась. Нашел на старом компе нужные папки, заархивировал, перетащил на новую машину, развернул через терминал - оказалось, что postgre по дефолту разворачивает на 5432, а на старом маке я разворачивал на 8000. Гуглил как сменить порт у моих tasks в postgresql, поменял. Установил proto, который не вставал на старой машине из-за старой версии оси, установил плагины
2. накосячил с пакетом pb: в proto был taskv1, а импортировал его как pb. сделал gosprints/internal/grpc/task/pb. Из api/proto/task/v1/task.proto создаются файлы в internal/grpc/task/pb/; он описывает все методы и структуры
3. была проблема в нессответствии типов id в proto и в model (в первом был string, во втором int). В proto сделал int32 и добавил конвертацию int32(id) / int(task.GetId())
4. добавил метод List в репозитории для работы сортировки, фильтра и пагниации
5. пока переписывал хэндлеры под grpc сломал контекст с user_id. Пришлось снова лезть в auth.GetUserFromJWT() и подружить все это с переписанными хэндлерами
6. сломалась структура конструктора NewTaskHandler из-за того что пока переписывал хендлеры убрал уведомления ws. Уведомлениям нужен был taskHandler с hub чтобы работали уведомления
7. сделал полнотекстовый поиск, но в процессе миграции postgresql опять вылезла проблема, связанная с тем что postgre не на своем дефолтном порту. Чтобы это понять пришлось прологировать client, server в gRPC и метод поиска в хэндлере. Еще оказалось что папка с файлом sql для миграции должна лежать в корне, а не в internal
8. Кэш не работает, потому что используется gRPC клиент напрямую, минуя сервисный слой с кэшированием. Я могу в main.go при запуске gRPC заменить baseTaskRepo на apiTaskRepo, но надо будет в taskCacheRepository добавить недостающие методы. А можно просто оставить все как есть, при этом кэш будет работать на уровне HTTP через apiTaskRepo, но gRPC будет ходить напрямую в БД. ОТВЕТ: HTTP за кэшом обращается к сервису через определенный интерфейс. gRPC может обращаться к сервису через такой же интерфейс
9. Начал разделять проект на task-service и notification-service. Набросал kafka producer в task-service, перетащил туда старый хэндлер с тасками, убрал оттуда прямые вызовы websocket. В папку notification-service перетащил весь ws из старого internal, накидал kafka consumer, в manager.go сделал шаблон для развития логики уведомлений пользователя по email. Накидал docker-compose, dockerfile для каждого сервиса. Убрал ws hub из структур хэндлеров, добавил в эти структуры eventProducer. Добавил cmd/main.go для каждого из сервисов. В main таск-сервиса создал kafka producer и создал taskHandler с этим kafka продюсером, убрал оттуда старый hub вебсокета. Начал растаскивать старый internal на два сервиса и собираю api-gateway. Путаюсь с импортами в новой архитектуре потому что до конца не понимаю как должен api-gateway взаимодействовать с двумя имеющимися сервисами. Пользователи и задачи у меня сейчас в одной БД и я пока не понимаю как сделать лучше: оставить пользователей в task-service (тогда auth-service будет ходить в task-service по gRPC), создать auth-service (наверное, самый правильный с точки зрения инкапсуляции), или же оставить пользователей в api-gateway (и тогда у gateway будет своя БД для users) ОТВЕТ: Пусть api-gateway обращается за пользователем в taskService
10. Разделил проект на task-service и notification-service. Набросал kafka producer в task-service, перетащил туда старый хэндлер с тасками, убрал оттуда прямые вызовы websocket. В папку notification-service перетащил весь ws из старого internal, накидал kafka consumer, в manager.go сделал шаблон для развития логики уведомлений пользователя по email. Накидал docker-compose, dockerfile для каждого сервиса. Убрал ws hub из структур хэндлеров, добавил в эти структуры eventProducer. Добавил cmd/main.go для каждого из сервисов. В main таск-сервиса создал kafka producer и создал taskHandler с этим kafka продюсером, убрал оттуда старый hub вебсокета. Растащил старый internal на два сервиса, собрал api-gateway. Поправил импорты в новой архитектуре, но до конца не понимаю как должен api-gateway взаимодействовать с двумя имеющимися сервисами. По твоему совету поместил юзеров в task-service, чтобы api-gateway брал пользователей оттуда. Создал api-gateway/internal/grpc/user_client.go, который работает с task-service. Собрал в каждом сервисе go.mod, но когда запускаю go build cmd/main.go гошка ищет пакеты в /usr/local/go/src/ и не может найти. В моих go.mod отсутствовала директива replace, поэтому гошка пытается скачать импорты из гита и получает 404. Replace добавил, но почему то гошка все равно лезет в сеть и go build cmd/main сыпется. Оказалось что в таск-сервисе в cmd/main не все лишние импорты убрал, при сборке гошка их искала и не могла найти (эти импорты подтянулись у меня из старого общего main, когда проект не был разделен на два сервиса, а сами пакеты я уже растаскал на task-service, notification-service и api-gateway). Весь проект пока что сыпется. Для меня перепиливать архитектуру проекта это всегда самое нервное занятие.
11. Добавил модели в новую архитектуру, но была проблема с путями генерации protoc. Перенастроил protoc и снова начались проблемы с импортами. Снова исправил и всплыла проблема с отсутствием метрик в воркере. Добавил метрики, потом при сборке build получал ошибку что пакета task-service в гите не существует, правил go.mod внутри этого микросервиса: в ссылке на модуль убрал ссылку на гит task-service (просто осталась ссылка на локальный task-service. Сам task-service по go.mod думал что он главный и зовут его gosprints). !Из task-service нужно было убирать все что связано с ws, auth, kafka producer + notifier, router, middleware. Auth, metrics, cash - в api-gateway. HTTP должен пойти через api-gateway. В процессе еще пришлось добавлять методы в соответствии с интерфейсом TaskRepository в task_cache_repository (list и search), заставляя работать кэш через grpc.
12. Собрал task-service, начались приколы с notification-service. Пришлось убирать auth, модели и метрики оттуда. Создал в notification-service proto-файлы, отрефакторил ws/handler и ws/hub для использования без id пользователя; убрал из notifiers/manager.go ws.Hub, EmailNotifier и ws.Message; в kafka/consumer передал hub. Notification-service собрался.
13. В api-gateway добавил pb для grpc. Добавил модели юзеров и тасок, папку repositories/interfaces в api-gateway. Убрал ws из router.go. Убрал из папки grpc user_client.go. Упростил handlers/auth.go: убрал оттуда зависимость от grpc клиента. Выяснил, что repositories/interfaces.go тоже не нужен, скорее всего. Добавил в cmd/main.go прокси-хэндлеры. В роутере указал прокси-хэндлеры на защищенных маршрутах, тут же добавил middleware из соответствующего и ранее имевшегося пакета. Этот middleware отрефакторил. Урезанный в момент организации архитектуры api-gateway handlers/task_proxy расширил недостающими методами (в роутере прокси работает на всех защищенных маршрутах). Убрал из handlers/auth импорт моделей. Снес из хэндлеров ws_proxy. Api-gateway собрался
14. поправил название бд в database внутри task-service. Kafka не запускалась без docker-copmose - установил. Посыпались ошибки атрибутов docker-compose.yml - упростил его. Вылезла ошибка project name must not be empty при запуске кафки. Оказалось что я забыл переустановить docker на новый комп. потом поймал ошибку docker-credential-desktop: executable file not found. Добавил в path, запустил docker вручную, запустил kafka и все заработало. Запускаю notification-service и получаю что Kafka не отвечает на порту 9092. Оказалось что kafka этот порт не слушает, потому что не был запущен соответствующий контейнер. Был запущен zookeeper, но не было правильного подключения kafka к нему. Оказалось, новая версия kafka требует переменную KAFKA_PROCESS_ROLES. Кафка запускалась но падала через несколько секунд, как и zookeeper. Проблема была с переменными окружения. Kafka + zookeeper с notification-service заработали
15. Вышла проблема с необходимостью перегенерировать proto в api-gateway. Далее была ошибка что api-gateway перестал слушать порт 8080 или же завис. В cmd/main.go расставил логи и увидел что api-gateway зависает при подключении к task-service - проблема с grpc-клиентом. При этом task-service работает и слушает 50051. api-gateway пытается подключиться к task-service:50051, но правильный адрес — localhost:50051. Исправил и все заработало
16. Когда отправляю задачу воркер падает с nil pointer dereference. В worker.go не хватает инициализации. Убрал оттуда вызовы notifier, оставшиеся со времен монолита. Убрал grpc.WithBlock() в функции NewTaskClient из api-gateway/internal/grpc/client.go. Микросервисная архитектура заработала. Все запросы тоже работают, но надо почистить корень проекта
